/**
 * Dashboards › Quality › AI Scoring
 *
 * Placeholder. Built in its own build-order step — see
 * docs/dashboards-quality-design.md §12. The route, access key and permission
 * mapping exist from step 4 so the nav shape can be reviewed and granted
 * before the page behind it does anything.
 */
import { renderPlaceholder } from "../../placeholder.js";

export default function render(ctx) {
  return renderPlaceholder(ctx);
}
