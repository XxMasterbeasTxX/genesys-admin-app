# Archy options file — DEMO org (source).
# Copy this to options.demo.yaml and fill in real values. Do NOT commit the real file.
#
# location values are Archy region ids, e.g.:
#   prod_us_east_1  (mypurecloud.com)
#   prod_eu_west_1  (mypurecloud.ie)
#   prod_eu_west_2  (mypurecloud.de / euw2 — verify with `archy dump` / docs)
#   prod_eu_central_1 (mypurecloud.de)
# Use the region that matches the demo org.

clientId: REPLACE_WITH_DEMO_CLIENT_ID
clientSecret: REPLACE_WITH_DEMO_CLIENT_SECRET
location: prod_eu_west_1
